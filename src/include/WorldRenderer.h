#pragma once

#include <vector>
#include <glm/glm.hpp>
#include "Chunk.h"

// Receives chunks from server
// Caches a number of chunks around the player
// Renders cached chunks
namespace Craft
{
	class WorldRenderer
	{
		public:
			WorldRenderer();

		private:

	};
}